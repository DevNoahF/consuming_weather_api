spring:
  application:
    name: WeatherApiConsuming
    ApiKey: ${API_KEY}
    cors:
      allowed-origins: "http://localhost:4200"
  cache:
    type: redis
    redis:
      time-to-live: 1h
  data:
    redis:
      port: 6379
      host: localhost
      client-name: cache
